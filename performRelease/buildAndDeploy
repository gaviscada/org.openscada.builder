#!/bin/bash

set -e

. profile

pushd workspace/org.openscada.builder

echo "Build profile: $buildProfile"

pushd superParent
mvn clean deploy -Pdeploy -P$buildProfile
popd

pushd aggregator
mvn clean deploy -Pdeploy -P$buildProfile
popd


popd
